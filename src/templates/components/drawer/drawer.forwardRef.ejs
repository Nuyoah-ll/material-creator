import { Drawer, DrawerProps } from "@ecom/auxo";
import React, { useImperativeHandle } from "react";

import styles from "./index.module.scss";

export interface <%= upperCamelCaseComponentName %>Props extends DrawerProps {
  selfDefinedProps?: any; // todo
}

export interface <%= upperCamelCaseComponentName %>Ref {
  // todo
}

const <%= upperCamelCaseComponentName %>: React.FC<<%= upperCamelCaseComponentName %>Props> = React.forwardRef<<%= upperCamelCaseComponentName %>Ref, <%= upperCamelCaseComponentName %>Props>((props, ref) => {
  const { selfDefinedProps, ...resProps } = props;

  useImperativeHandle(ref, () => ({
    // todo
  }));

  return (
    <Drawer className={styles.<%= lowerCamelCaseComponentName %>} {...resProps}>
      <%= upperCamelCaseComponentName %>
    </Drawer>
  );
});

export default <%= upperCamelCaseComponentName %>;
